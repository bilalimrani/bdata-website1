import styled from "styled-components";

const LightWeightSectionWrapper = styled.div`
  padding: 50px 0 100px;
`;

const Section2 = styled.div`
  p {
    padding-bottom: 35px;
  }
`;

const Button = styled.button``;

export { LightWeightSectionWrapper, Section2, Button };
